import React from 'react';

// TODO (next phase): port web src/components/Gamification/ChatWidget.jsx (or
// src/components/ChatWidget.jsx) here. Kept as a no-op so App.js structure
// matches App.jsx exactly without breaking the build.
export default function ChatWidget() {
  return null;
}
