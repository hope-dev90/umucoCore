import React from 'react';

// TODO (next phase): port web src/components/Gamification/BadgeUnlockToast.jsx (or
// src/components/BadgeUnlockToast.jsx) here. Kept as a no-op so App.js structure
// matches App.jsx exactly without breaking the build.
export default function BadgeUnlockToast() {
  return null;
}
