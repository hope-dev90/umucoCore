import React from 'react';

// TODO (next phase): port web src/components/Gamification/RewardToastContainer.jsx (or
// src/components/RewardToastContainer.jsx) here. Kept as a no-op so App.js structure
// matches App.jsx exactly without breaking the build.
export default function RewardToastContainer() {
  return null;
}
