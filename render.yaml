services:
  - type: web
    name: umucocore
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_BASE
        value: https://umucocore.onrender.com
